import React from "react"
import tw from "tailwind-styled-components"

const Post = ({ displayName, username, verified, text, image, avatar }) => {
  return <PostContainer></PostContainer>
}

export default Post

const PostContainer = tw.article`

`
